Nginx 配置模板


#user  nobody;
worker_processes  1;
#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;
#pid        logs/nginx.pid;
 
events {
    worker_connections  1024;
}
 
http {
    include       mime.types;
    include       vhost/*.conf;
    default_type  application/octet-stream;
    add_header 'Access-Control-Allow-Origin' '*';
    add_header 'Access-Control-Allow-Credentials' 'true';
    add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';
    #access_log  ../logs/access.log  main;
    sendfile        on;
    #tcp_nopush     on;
    #keepalive_timeout  0;
    keepalive_timeout  65;
    #gzip  on;


    server {
        listen       80;
        server_name  fsbox.com;
        return      301 https://$server_name$request_uri;
        location /{
            root  /data/net/html/front;
            try_files $uri $uri/ @router;
            index  index.html index.htm;
            if ($request_filename ~* ^.*?\.(txt|doc|pdf|rar|gz|zip|docx|exe|xlsx|ppt|pptx)$){
                add_header Content-Disposition: 'attachment;';
            }
        } 
        location @router {
            rewrite ^.*$ /index.html last;
        }
        location /api/ {
            proxy_pass http://127.0.0.1:5000/api/; 
            proxy_set_header Host $http_host;
            proxy_cookie_path / /;
            proxy_cookie_domain localhost:5000 localhost;
        }


        
       
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
    }   
    server {
        listen       81;
        server_name  box.fs.com;        
        return      301 https://$server_name$request_uri;
        location /{
            root  /data/net/html/back;
            try_files $uri $uri/ @router;
            index  index.html index.htm;
            if ($request_filename ~* ^.*?\.(txt|doc|pdf|rar|gz|zip|docx|exe|xlsx|ppt|pptx)$){
                add_header Content-Disposition: 'attachment;';
            }
        }
        location @router {
            rewrite ^.*$ /index.html last;
        }
        
        
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
    }   
 
    # HTTPS server
    #
    #
    server {
        listen       443 ssl;
        server_name  fsbox.com 54.203.155.188;
        ssl_certificate ssl/1_fsbox.com_bundle.crt;
        ssl_certificate_key  ssl/2_fsbox.com.key;
        ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;
        ssl_ciphers  HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers  on;
        location / {
            root  /data/net/html/front;
            try_files $uri $uri/ @router;
            index  index.html index.htm;
        if ($request_filename ~* ^.*?\.(txt|doc|pdf|rar|gz|zip|docx|exe|xlsx|ppt|pptx)$){
        add_header Content-Disposition: 'attachment;';
        }
        }
        location @router {
            rewrite ^.*$ /index.html last;
        }
        location /api/ {
        proxy_pass http://127.0.0.1:5000/api/; 
        proxy_set_header Host $http_host;
        proxy_cookie_path / /;
        proxy_cookie_domain localhost:5000 localhost;
        }
        
        location /apifs/ {
        proxy_pass http://www.fs.com/;
        proxy_set_header Host $http_host;
        proxy_cookie_path / /;
        proxy_cookie_domain test.whgxwl.com:8000/ localhost;
        }
    }
 
    # HTTPS server
    #
    #
    server {
       listen       443 ssl;
       server_name  box.fs.com;
       ssl_certificate ssl/1_box.fs.com_bundle.crt;
       ssl_certificate_key  ssl/2_box.fs.com.key;
        ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;
        ssl_ciphers  HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers  on;
        location / {
            root  /data/net/html/back;
            try_files $uri $uri/ @router;
            index  index.html index.htm;
        if ($request_filename ~* ^.*?\.(txt|doc|pdf|rar|gz|zip|docx|exe|xlsx|ppt|pptx)$){
        add_header Content-Disposition: 'attachment;';
        }
        }
        location @router {
            rewrite ^.*$ /index.html last;
        }
        location /backapi/ {
        proxy_pass http://0.0.0.0:5001/backapi/; 
        proxy_set_header Host $http_host;
        proxy_cookie_path / /;
        proxy_cookie_domain localhost:5001 localhost;
        }
    }
  
 
    }